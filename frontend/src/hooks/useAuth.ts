/**
 * useAuth Hook
 * ============
 * Re-exports useAuth from AuthContext for convenience.
 * This pattern keeps hook imports consistent across the app.
 */
export { useAuth } from '../contexts/AuthContext'
